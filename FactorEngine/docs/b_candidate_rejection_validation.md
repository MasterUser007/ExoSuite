# B. Candidate Rejection Validation

One of the core functions of the toolkit is to validate that composite candidates are rejected at the correct stage based on available symbolic patterns and numeric behavior. Stage-by-stage rejection logs are provided in hashed form, ensuring data privacy while enabling deterministic comparison against reference verdicts.

